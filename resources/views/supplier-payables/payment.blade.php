<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold">Registrar pago · {{ $supplierPayable->description }}</h2></x-slot><div class="p-4 sm:p-6"><form method="POST" action="{{ route('supplier-payables.pay',$supplierPayable) }}" class="mx-auto max-w-xl space-y-4 rounded bg-white p-6 shadow">@csrf<p>Saldo pendiente: <strong>${{ number_format($supplierPayable->balance,2) }}</strong></p><label class="block">Fecha<input type="date" name="transaction_date" required value="{{ old('transaction_date',today()->format('Y-m-d')) }}" class="mt-1 w-full rounded border"></label><label class="block">Monto<input type="number" name="amount" min="0.01" max="{{ $supplierPayable->balance }}" step="0.01" required value="{{ old('amount') }}" class="mt-1 w-full rounded border"></label><label class="block">Método<input name="method" value="{{ old('method') }}" class="mt-1 w-full rounded border"></label><label class="block">Descripción<textarea name="description" class="mt-1 w-full rounded border">{{ old('description') }}</textarea></label>@if($errors->any())<div class="text-red-700">{{ $errors->first() }}</div>@endif<button class="rounded bg-green-700 px-4 py-3 text-white">Registrar pago</button></form></div></x-app-layout>
