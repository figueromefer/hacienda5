<x-app-layout><x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-2"><div><h2 class="text-xl font-semibold">{{ $supplierPayable->description }}</h2><p>{{ $supplierPayable->supplier->name }}</p></div><div class="flex gap-2"><a href="{{ route('supplier-payables.index') }}" class="rounded bg-gray-200 px-4 py-3">Volver</a>@if(!in_array($supplierPayable->status,['cancelled','paid']))<a href="{{ route('supplier-payables.payment',$supplierPayable) }}" class="rounded bg-green-700 px-4 py-3 text-white">Registrar pago</a>@endif</div></div></x-slot><div class="p-4 sm:p-6"><div class="grid gap-5 lg:grid-cols-2"><div class="rounded bg-white p-6 shadow"><dl class="grid grid-cols-2 gap-4"><div><dt>Total</dt><dd class="text-xl font-bold">${{ number_format($supplierPayable->total_amount,2) }}</dd></div><div><dt>Pagado</dt><dd class="text-xl font-bold">${{ number_format($supplierPayable->paid_amount,2) }}</dd></div><div><dt>Saldo</dt><dd class="text-xl font-bold">${{ number_format($supplierPayable->balance,2) }}</dd></div><div><dt>Estado</dt><dd>{{ $supplierPayable->status_label }}</dd></div><div><dt>Concepto</dt><dd>{{ $supplierPayable->expenseConcept?->name ?? '-' }}</dd></div><div><dt>Evento</dt><dd>{{ $supplierPayable->event?->title ?? '-' }}</dd></div><div><dt>Vencimiento</dt><dd>{{ $supplierPayable->due_date?->format('d/m/Y') ?? '-' }}</dd></div></dl><p class="mt-5 whitespace-pre-line">{{ $supplierPayable->notes }}</p>@if($supplierPayable->status !== 'cancelled')<form method="POST" action="{{ route('supplier-payables.cancel',$supplierPayable) }}" class="mt-6" onsubmit="return confirm('La cuenta se cancelará y sus pagos se conservarán. ¿Continuar?')">@csrf @method('PATCH')<input type="hidden" name="confirm_cancel" value="1">@if($supplierPayable->status==='paid')<label><input type="checkbox" name="confirm_paid_cancel" value="1" required> Confirmo cancelar esta cuenta ya liquidada</label>@endif<button class="block mt-2 text-red-700">Cancelar cuenta</button></form>@endif</div><div class="rounded bg-white p-6 shadow"><h3 class="font-bold">Historial de pagos</h3>@forelse($payments as $payment)<div class="mt-3 border-t pt-3"><a href="{{ route('transactions.show',$payment) }}" class="font-medium text-blue-700">{{ $payment->reference }}</a> · ${{ number_format($payment->amount,2) }} · {{ $payment->transaction_date->format('d/m/Y') }}<div>{{ $payment->method ?: 'Sin método' }} · {{ $payment->status }}</div></div>@empty<p class="mt-3 text-gray-500">Aún no hay pagos.</p>@endforelse</div></div></div></x-app-layout>
